---
RecipeFormatVersion: '2020-01-25'
ComponentName: aws.greengrass.Cli
ComponentDescription: This component provides CLI access to the Nucleus via IPC. It mostly wraps IPC functionality (see Device SDK) with trivial code, and effectively provides further documentation. It also makes the open source GGC usable.
ComponentPublisher: AWS
ComponentVersion: '0.0.0'
ComponentType: aws.greengrass.plugin
ComponentConfiguration:
  DefaultConfiguration:
    AuthorizedPosixGroups: ""
Manifests:
  - Platform:
      os: linux
    Lifecycle:
      install:
        script: source "{{artifacts:decompressedPath}}/aws.greengrass.cli.client/install.sh"
    Artifacts:
      - URI: s3://gg-dev-artifacts-$stage/$componentName/$platform.os/$version/aws.greengrass.Cli.jar
      - URI: s3://gg-dev-artifacts-$stage/$componentName/$platform.os/$version/aws.greengrass.cli.client.zip

